name: RDWAN Tech Backup Publisher
# Preparation/recovery watchdog. These runs occupy the gap between primary
# publication slots: they prepare and quality-check the next bilingual story,
# retry preparation when needed, and publish only when a 20-minute slot is due
# and the primary publisher missed it.

on:
  workflow_dispatch:
  schedule:
    - cron: '7,12,17,27,32,37,47,52,57 * * * *'

permissions:
  contents: write
  copilot-requests: write

concurrency:
  group: rdwan-tech-publication-write
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 22
    env:
      GITHUB_TOKEN: ${{ github.token }}
      COPILOT_GITHUB_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: automation/forum/requirements.txt

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install dependencies
        run: |
          python -m pip install --disable-pip-version-check -r automation/forum/requirements.txt
          npm install -g @github/copilot

      - name: Run pipeline tests
        run: python -m unittest discover -s automation/forum/tests -v

      - name: Collect and evaluate fresh stories
        run: python automation/forum/run.py

      - name: Prepare next story or recover a missed due slot
        run: python automation/forum/publisher_scheduler.py

      - name: Build crawlable Arabic and English forum hubs
        run: python automation/forum/build_locale_pages.py

      - name: Finalize SEO indexability
        run: python automation/forum/seo_finalize.py

      - name: Normalize canonical host across HTML
        run: python automation/seo/normalize_html.py

      - name: Upload backup audit snapshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rdwan-tech-backup-${{ github.run_number }}
          path: |
            automation/forum/state/run_report.json
            automation/forum/state/publisher_report.json
            automation/forum/state/prepared_article.json
            automation/forum/state/candidates.json
          retention-days: 14
          if-no-files-found: warn

      - name: Commit publication and preparation state
        shell: bash
        run: |
          git config user.name "RDWAN Tech Automation"
          git config user.email "actions@users.noreply.github.com"
          git add forum sitemap.xml automation/forum/state/*.json
          git add -- ':(glob)**/*.html'
          if git diff --cached --quiet; then
            echo "Backup preparation/recovery cycle completed; no repository changes were required."
            exit 0
          fi
          git commit -m "RDWAN Tech: prepare or recover next 20-minute story"
          git pull --rebase origin main
          git push origin HEAD:main
