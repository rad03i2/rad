# Brand Assets

This directory contains visual identity assets for brands used by the project.

## Mikhbar
Upload the complete `mikhbar` brand folder here so its final path is:

`assets/brand/mikhbar/`

Keep Mikhbar assets separate from the personal-site assets under `assets/images/` and `assets/social/`.
