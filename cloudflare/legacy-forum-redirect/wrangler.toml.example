name = "mikhbar-legacy-forum-redirect"
main = "src/index.js"
compatibility_date = "2026-09-24"
workers_dev = false

# IMPORTANT:
# Keep this route disabled in source until the rdwan.dev zone change is explicitly approved.
# When enabled, it must be scoped ONLY to the old publication path so the personal site is untouched.
#
# [[routes]]
# pattern = "rdwan.dev/forum*"
# zone_name = "rdwan.dev"
